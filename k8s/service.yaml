# 프론트 파드가 이 이름으로 백엔드를 찾는다(BACKEND_URL=http://egov-simple-api:8090).
apiVersion: v1
kind: Service
metadata:
  name: egov-simple-api
  labels:
    app: egov-simple-api
spec:
  type: ClusterIP
  selector:
    app: egov-simple-api
  ports:
    - name: http
      port: 8090
      targetPort: http
